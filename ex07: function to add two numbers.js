function add(numOne,numTwo){
  const ans = numOne+numTwo
  console.log(ans)
}
const first = parseInt(prompt('Enter the first number '));
const second = parseInt(prompt('Enter the second number '));
// var first = readlinesync.question("First number is:");
// var second = readlinesync.question("Second question is: ");

add(first,second)
